<%= form_for(@action_with_comment) do |f| %>
  <% if @action_with_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@action_with_comment.errors.count, "error") %> prohibited this action_with_comment from being saved:</h2>

      <ul>
      <% @action_with_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.text_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
